package Controllers.CustomElements;

public class VerticalArrow extends Arrow {
    public VerticalArrow(double startX, double startY) {
        super(startX + 62.5, startY, startX + 62.5, startY + 50, 10.0);
    }
}
